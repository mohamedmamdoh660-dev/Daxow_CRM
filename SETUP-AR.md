# تشغيل التطبيق محلياً - الحالة الحالية

## ✅ تم بنجاح

التطبيق يعمل الآن على:
**http://localhost:3000**

## ⚠️ يحتاج إعداد قاعدة البيانات

التطبيق يعمل لكن صفحات البيانات (مثل `/students`) تحتاج قاعدة بيانات PostgreSQL.

## 📋 خيارات الإعداد

### الخيار 1: استخدام قاعدة بيانات السحابية (الأسهل) ⭐

**Supabase** (مجاني):
1. اذهب إلى [supabase.com](https://supabase.com)
2. سجل حساب مجاني
3. أنشئ مشروع جديد
4. انسخ "Connection string" (Direct connection)
5. حدّث الملف `prisma.config.ts` سطر 12:
   ```typescript
   url: "postgresql://postgres.xxx:[PASSWORD]@aws-0....supabase.com:6543/postgres",
   ```

6. نفذ هذه الأوامر:
   ```bash
   npx prisma migrate dev --name init
   npx prisma generate
   npx prisma db seed
   ```

### الخيار 2: PostgreSQL محلي (يحتاج كلمة مرور)

### 3. إعداد التخزين (Supabase Storage) 📂

1. اذهب إلى قائمة **Storage** في لوحة تحكم Supabase.
2. أنشئ Bucket جديد باسم: `crm-uploads`
3. اجعله **Public** (عام).
4. (اختياري) حدد حجم الملف بـ 10MB والأنواع المسموحة (صور، PDF).
5. سيقوم النظام بتنظيم الملفات تلقائياً داخل مجلدات `students/` و `leads/`.

### الخيار 2: PostgreSQL محلي (يحتاج كلمة مرور)

قاعدة البيانات المحلية تحتاج إعداد:

1. افتح pgAdmin أو PostgreSQL CLI
2. أنشئ قاعدة بيانات اسمها `admission_crm`
3. أنشئ مستخدم أو استخدم المستخدم الحالي
4. حدّث `prisma.config.ts`:
   ```typescript
   url: "postgresql://postgres:PASSWORD@localhost:5432/admission_crm",
   ```

5. نفذ الأوامر:
   ```bash
   npx prisma migrate dev --name init
   npx prisma generate
   npx prisma db seed
   ```

## 🧪 اختبار التطبيق الآن

حالياً، الصفحة الرئيسية تعمل:
- ✅ http://localhost:3000 - تعمل
- ⚠️ http://localhost:3000/students - تحتاج قاعدة بيانات

بعد إعداد قاعدة البيانات:
- جميع الصفحات ستعمل
- يمكنك إضافة طلاب
- يمكنك رؤية الحقول المخصصة

## 📺 التسجيل المرئي

شاهد التسجيل المرئي لحالة التطبيق الحالي:
![عرض التطبيق](file:///Users/mdarwish/.gemini/antigravity/brain/79bfdb5e-837c-4a30-8da0-5fabb21bd01f/app_running_demo_1767964848378.webp)

## 🚀 الخطوة التالية

**أوصي باستخدام Supabase** (مجاني وسريع، لا يحتاج إعداد):
1. سجل في supabase.com
2. أنشئ مشروع
3. انسخ Connection String
4. حدّث prisma.config.ts
5. نفذ الأوامر أعلاه
6. التطبيق سيعمل بالكامل! ✨
