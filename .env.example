DATABASE_URL="postgresql://username:password@localhost:5432/admission_crm?schema=public"

# When deploying, replace the above with your actual database URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA

# Example for local development:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/admission_crm"

# Example for production (e.g., Supabase, Neon, Railway):
# DATABASE_URL="postgresql://user:password@host.region.provider.com:5432/database"
